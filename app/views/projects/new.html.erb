<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Points App</title>
  </head>
  <body>
    <h2 class="new_title">Create New Project</h2>

    <%= form_for(@project) do |f| %>
      <div class="field">
        <%= f.text_field :title, placeholder: "Project Title", class: "project_title" %>
      </div>
      <%= f.submit "Create", id: "project_button" %>
    <% end %>

  </body>
</html>
